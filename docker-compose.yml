version: '3.8'

services:
  frontend-user:
    build:
      context: ./frontend-user
      dockerfile: Dockerfile
    container_name: taobao-frontend-user
    ports:
      - "8081:80"
    restart: unless-stopped
    networks:
      - taobao-network

networks:
  taobao-network:
    driver: bridge
